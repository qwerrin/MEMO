# SQLAlchemyメモ
### SQLAlchemyとは
*** 
pythonのORMモジュール。  
session.query(User).all()  
このようにSQL操作ができる。  
  
### ORM
***
SQLをクラスとして扱えるようにしたもの。
* SQLインジェクション対策がサポートされる。
* クラスなので、SQLをオブジェクト指向で書ける。
* 引数に変数を入れるため、文字列の結合などが必要ないので短く書ける。

### 設定
***
別途ソースを参照

### 継承etc...
***
SQLAlchemy1.3Documentationより、
* 水平シャープ
  * 複数のデータベースにわたってデータを水平方向に拡大縮小すること。
分割マッピングの基本的な要素は次のとおり。
* それぞれ「シャードID」が割り当てられた複数のデータベース
* 保存するインスタンスが与えられたときに、単一のシャードIDを返すことができる関数。これは“ shard_chooser”と呼ばれる。
* 特定のインスタンス識別子に適用されるシャードIDのリストを返すことができる関数。これは「id_chooser」と呼ばれます。すべてのシャードIDが返される場合は、* すべてのシャードが検索される。
* 特定のQuery（“ query_chooser”）が与えられたときに、試すシャードIDのリストを返すことができる関数。すべてのシャードIDが返されると、すべてのシャードが照会され、結果が結合される。
* クラス継承階層のマッピング
  * 結合テーブル継承
    * 
  









